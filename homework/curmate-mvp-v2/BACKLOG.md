# BACKLOG.md — งานที่ยังไม่ทำ (Curmate MVP v2)

รายการนี้สรุปจาก [spec.md](./spec.md) หมวด "ขอบเขตที่ยังไม่ทำ" — จัดลำดับความสำคัญคร่าวๆ สำหรับ sprint ถัดไป

| # | งาน | อ้างอิง | ความสำคัญ | หมายเหตุ |
| --- | --- | --- | --- | --- |
| 1 | เทสต์ Playwright ข้อ 1/2 ยังรันไม่สำเร็จ — รอเลื่อน role บัญชีทดสอบเป็น ADMIN | [test-results.md](./test-results.md) | **สูง** | ไม่ใช่บั๊ก เป็นข้อจำกัด precondition (ต้องเลื่อน `playwright-admin-w9@curmate-test.dev` เป็น ADMIN ด้วยมือผ่าน Firebase Console ก่อน) — เมื่อเสร็จจะรันผ่านหมด ดู "หมายเหตุสำคัญ" ใน test-results.md |
| 2 | Soft-delete / ถังขยะ / undo การลบข้อมูล | spec.md หมวด 7 | กลาง | ปัจจุบันลบแล้วกู้คืนไม่ได้ (ไม่มี trash/undo ใน Firestore test mode) ต้องเพิ่ม collection เหมือน `deletedCriteriaSets` หรือ soft-delete flag |
| 3 | STAFF: "นำหลักสูตรเข้าตรวจสอบความสอดคล้อง" | spec.md หมวด 7, ACL.md | ต่ำ | คนละฟีเจอร์กับ criteria-knowledge-base (ของการบ้านนี้) — เป็นฟีเจอร์ "tqf2-consistency-check" ของโปรเจกต์หลัก Curmate ออกแบบไว้เสมอ แต่ยังไม่ implement |
| 4 | STAFF: "เลือกใช้ชุดเกณฑ์" (กำหนดว่า STAFF นี้ใช้ชุดไหน) | spec.md หมวด 7, ACL.md | ต่ำ | เหมือนข้อ 3 ออกแบบไว้แต่ยังไม่ implement — ต้องเพิ่ม collection `staffAssignedCriteriaSets` หรือ pivot table นึก |
| 5 | Firebase Storage (ถอดออกแล้ว) | spec.md หมวด 7 | ต่ำ | เคยเพิ่มแล้วถอดออกสัปดาห์ที่ 8 เพราะ Storage ต้องแผน Blaze — เปลี่ยนไปใช้ลิงก์ Google Drive เก็บ PDF ต้นฉบับแทน (ผู้ใช้อัปโหลดเอง ไม่ผ่าน Firebase) |
| 6 | ย้ายคีย์ AI ไปไว้ฝั่งที่ผู้ใช้แตะไม่ได้ | [CLAUDE.md](./CLAUDE.md) (หัวข้อ "ผู้ช่วย AI"), [spec.md](./spec.md) (หมวด Security) | **สูง** | คีย์ OpenRouter ถูกโหลดผ่าน `js/ai-config.local.js` ที่ฝั่ง client ตรงๆ (ผ่าน `<script>` ในหน้า HTML) แม้ว่าไม่ถูก push ขึ้น GitHub (`.gitignore` ครอบแล้ว) แต่มองเห็นได้จาก browser devtools/network tab ของทุกคนที่เข้าเว็บ เพราะระบบนี้ไม่มี backend เลย — แนวทางแก้: ย้ายการเรียก AI ไปอยู่หลัง serverless function/backend เพื่อไม่ให้คีย์ถูกส่งลง client |
| 7 | เก็บเลขหน้า/ช่วงหน้า + ข้อความต้นฉบับของกฎเกณฑ์ (verbatim) | [spec.md](./spec.md) หมวด "ฟีเจอร์ AI" FR ข้อ 10 ของหัวข้อ "จัดการฐานความรู้เกณฑ์" | กลาง | ฟีเจอร์ 06 (สร้างชุดเกณฑ์) ยังไม่มี: เพิ่มข้อมูลตำแหน่งต้นทาง (เลขหน้าหรือช่วงหน้าที่กฎปรากฏ) และข้อความต้นฉบับแยกต่างหากจากข้อความที่ AI สรุป — ปัจจุบัน MVP มี `sourceRef` (เลขข้อ/มาตรา) เท่านั้น ไม่มีเลขหน้า และไม่มี verbatim แยกเก็บ จำเป็นสำหรับฟีเจอร์ 07 (หน้าตรวจสอบ/อนุมัติ) และฟีเจอร์ 09 (แชทบอท) |
| 8 | ติดป้ายกำกับและเตือนเอกสารที่แปลงจากภาพสแกน (OCR) | [spec.md](./spec.md) หมวด "ฟีเจอร์ AI" FR ข้อ 11 ของหัวข้อ "จัดการฐานความรู้เกณฑ์" | กลาง | ฟีเจอร์ 06/07 ยังไม่มี: ระบบต้องติดป้ายกำกับเอกสารที่อัปโหลดและแปลงจากภาพสแกน (OCR) แล้วเตือนแอดมินในหน้า 07 (ตรวจสอบ/อนุมัติ) ให้ตรวจสอบเป็นพิเศษ เนื่องจากมีโอกาสสกัดคลาดเคลื่อนสูงกว่าเอกสารข้อความดิจิทัล — ป้ายนี้สืบทอดไปยังกฎเกณฑ์ทุกข้อที่สกัดจากเอกสารนั้น |
| 9 | ชุดเทสต์ยังไม่ครอบคลุมเคส data-isolation (5d) ที่ STAFF เปิดชุด pending ผ่านการแก้ hash | [test-results.md](./test-results.md) (หัวข้อ "เคส 5d") | ต่ำ | ไม่มีบั๊กที่เทสต์ Playwright จับได้ (5/5 สถานการณ์หลักผ่านหมด) มีเพียง 1 เคสย่อยของ data-isolation ที่ชุดเทสต์ยัง**ข้าม**ไม่ครอบคลุม (5d: ตั้งใจ login เป็น STAFF แล้วแก้ URL hash เปิดหน้า 07 ของชุดเกณฑ์สถานะ pending ตรงๆ) — เป็นช่องว่างของชุดเทสต์เอง ไม่ใช่บั๊กที่พบในแอป (ต้อง setup step ให้เทสต์สร้าง criteriaSet สถานะ pending ขึ้นมาเอง ก่อนรันเคส 5d ได้) |
